FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt requirements.txt

RUN pip install --upgrade pip && pip install -r requirements.txt

COPY serving/ serving/

CMD ["sh", "-c", "uvicorn serving.api:app --host 0.0.0.0 --port ${PORT}"]